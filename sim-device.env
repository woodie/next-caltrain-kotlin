# Default Android emulator AVD sim.sh boots when no -d/--device override is
# given on the command line. Edit + commit this file to change the default
# for everyone; for a personal-only override, set SIM_DEVICE in local.env
# instead (gitignored, takes precedence over this file).
SIM_DEVICE="Pixel_8"
