# Committed app configuration -- defaults that ship with the app unless
# overridden per-developer in local.properties (gitignored). Edit and commit
# this file directly to change a default for everyone (e.g. if the schedule
# data ever moves to a new home). Room for more keys here later; not
# schedule-only.
#
# For local testing without touching this file, override via `scheduleUrl=`
# in local.properties instead -- that takes precedence over this file.
# See docs/COWORK.md "Schedule data".
scheduleUrl=https://next-caltrain-pwa.appspot.com/feed/schedule.json
